import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxResultAge: {
        flex: 1,
        marginTop: 0,
        paddingTop: 10,
        borderRadius: 50,
        alignItems: "center",
        width: "100%",
        height: "auto",
    },
    textMessageResultAge: {
        fontSize: 18,
        color: "#000000",
        fontWeight: "bold",
    },
    textResultAge: {
        fontSize: 30,
        color: "#000000",
        fontWeight: "bold",
    },
})

export default styles